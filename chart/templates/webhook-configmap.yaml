apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-configmap
data: 
  MINIO_ENDPOINT: {{ .Values.webhook.configMap.endpoint }}
  MINIO_BUCKET_NAME: "fungjai"
  MINIO_LOCATION: "ap-southeast-1"
  MINIO_FOLDER_NAME: "responses"
  MINIO_RESPONSE_TYPE: "mood"